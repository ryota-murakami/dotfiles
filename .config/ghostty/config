# Ghostty Config

# Font size (in points)
font-size = 18

# Background opacity (0.0 to 1.0)
# 1.0 = opaque, 0.0 = fully transparent
background-opacity = 0.8

# Background color (change as you like)
# background = 1e1e2e

background-blur = 16